[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vrp"
version = "0.1.0"
description = "Exact MILP and Clarke-Wright heuristic solvers for CVRP, VRPTW, and MDVRP"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]